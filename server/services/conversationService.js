const partticipantModel=require("../models/participant")
const conversationModel=require("../models/conversation")

const conversationService={
    createConversation:async(participants,conversation )=>{
        
    }
}
